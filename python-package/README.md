# xgp Python package

## Installation

```sh
git clone github.com/MaxHalford/xgp
cd xgp/python-package
go get
go build -buildmode=c-shared -o xgp.so *.go
```

## Development

```sh
pip install -e .[dev]
```
